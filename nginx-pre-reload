#!/usr/bin/env bash
set -eo pipefail; [[ $DOKKU_TRACE ]] && set -x

# Restore SELinux context for nginx config
restorecon -v "$DOKKU_ROOT/$APP/nginx.conf"
restorecon -rv "$DOKKU_ROOT/$APP/nginx.conf.d"

